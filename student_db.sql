CREATE DATABASE student_db;
USE student_db;

CREATE TABLE Student (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         name VARCHAR(50),
                         mark INT
);